server:
   servlet:
      context-path: /crypto
   error:
      whitelabel:
         enabled: false
spring:
   application:
      name: cryptoOrchestrator
   datasource:
      url: jdbc:sqlserver://192.168.12.71:1433;databaseName=RipioQA;encrypt=false;trustServerCertificate=false;
      username: ${EMPLEADORES_BACK_DATABASE_USER:RipioQA}
      password: ${EMPLEADORES_BACK_DATABASE_PASS:RipioQA2026}
      driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
      #driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
   jpa:
      properties:
         hibernate:
            format_sql: true
            dialect: org.hibernate.dialect.SQLServerDialect
         database-platform: org.hibernate.dialect.SQLServerDialect
      show-sql: true
springdoc:
   swagger-ui:
      operations-sorter: alpha
      tags-sorter: alpha
apis:
   esco-bolsa:
      protocolo: http
      dominio: 192.168.12.51:6003
      api-version: 9
      urls:
         login: /api/v9/login
         detalleCuenta: /api/v9/get-detalle-cuenta
         tenencias: /api/v9/get-tenenciaval
   crypto-provider:
      ripio:
        protocolo: http
        dominio: localhost:8093
        urls:
           cliente-alta: /cripto-provider/ripio/cliente/
           cotizacion-alta: /cripto-provider/ripio/cotizacion/
           operacion-alta: /cripto-provider/ripio/operacion/
           cotizacion-consul: /cripto-provider/ripio/cotizacion/
login:
   token:
      header: Authorization
      secreto: Scre-to.159-AuZ-tero.753xFRD345dfr#456x
      expiracion: 600000 #milisegundos 600000 
      isuer: DAValores
   tokenRefresco:
      expiracion: 1200000 
   dfa:
      issuer: DAValores
      password: Dfa-to.159-AuZ-tero.753xFRD345dfr#456x